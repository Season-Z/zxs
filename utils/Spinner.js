const ora = require('ora')

class Spinner {
  constructor() {
    this.symbol = symbol;

    this.spinnerMap = {}
    // this.spinner = ora();
    // this.spinner.text = ` ${text}`;
  }

  start() { }

  static symbol() {
    return this.symbol
  }
}

module.exports = new Spinner()